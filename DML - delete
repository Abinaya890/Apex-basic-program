public class del {
    public static void delmeth()
    {
        list<student__c> slist = [SELECT Name FROM student__c WHERE isDeleted=true ALL ROWS];
            undelete list;
system.debug(slist);
         }

}
